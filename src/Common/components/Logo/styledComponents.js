import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const IBHUBSLogo = styled.img`
    ${tw`cursor-pointer`}
    width: 60px;    
    height: 60px;
    object-fit: contain;
`

export {IBHUBSLogo}